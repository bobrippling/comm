multicast host/client

windows:
	windows - unknown publisher - run don't run UAC
	privmsg - show in new window OPTIONAL

protocol:
	/nudge
	integrate file transfer
	drawing
	"Tim is typing"
	edit previous message (index - 1, ONLY) like Skype
	devmode - caps lock + rename

server:
	dump last 5 lines of chat when a new client connects

gui:
	move-to-$DISPLAY functionality
